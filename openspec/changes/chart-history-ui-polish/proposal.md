# Chart history & UI polish

- Add **4h, 1d, 1w, 1M** intervals with larger `limit` (~1y+ for daily/weekly/monthly).
- Two-row toolbar: symbol search on its own row to avoid overlap with chart-type buttons.
- **No auto fit** on poll: refit only when `symbol`, `interval`, or `chartType` changes (`viewRevision`).
- Keyboard **1–8** for eight timeframes; header hint updated.
