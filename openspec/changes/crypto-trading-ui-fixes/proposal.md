# Crypto Trading UI fixes

- Fix chart viewport height so candlesticks render visibly.
- Rebrand visible title to **Crypto Trading**.
- Add **Candle** vs **Line** chart toggle (close price line + volume).
- Extend `/market/symbols` with **`top_by_volume`** (100 USDT spot pairs by 24h `quoteVolume`).
